import Vue from 'vue';
import { expect } from 'chai';
import GetData from '@/components/GetData.vue'
import { mount } from '@vue/test-utils';

// mocha + axios 获取数据 但数据不存在
// mocha + axios = moxios
describe('测试异步获取数据', () => {
  it('', () => {})
})